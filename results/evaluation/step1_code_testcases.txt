**Test‑Case Suite for the *Virtual Clinic* models**  
*(Only the models present in the supplied `models.py` are exercised. No views, forms or URLs exist in the provided code, therefore no test cases target those components.)*  

---  
**TC-ID**          : TC‑CODE‑001  
**Title**          : Verify `Speciality.__str__` returns the speciality name  
**Target**         : Model – `Speciality`  
**Preconditions**  :  
  - Django test database is ready.  
**Test Steps**     :  
  1. Create a `Speciality` instance with `name="Cardiology"` and any description.  
  2. Call `str(instance)`.  
**Expected Result**: The string returned is exactly `"Cardiology"`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑002  
**Title**          : `Speciality` field length enforcement (boundary)  
**Target**         : Model – `Speciality.name` / `Speciality.description`  
**Preconditions**  :  
  - None.  
**Test Steps**     :  
  1. Attempt to create a `Speciality` with a `name` of 51 characters (exceeds `max_length=50`).  
  2. Save the instance.  
**Expected Result**: Django raises a `DataError` / `IntegrityError` on save because the field length exceeds the defined limit.  
**Test Type**      : Boundary  

---  
**TC-ID**          : TC‑CODE‑003  
**Title**          : Verify `Symptom.__str__` returns the symptom name  
**Target**         : Model – `Symptom`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Symptom` with `name="Headache"` and any description.  
  2. Evaluate `str(symptom)`.  
**Expected Result**: Returns `"Headache"`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑004  
**Title**          : `Location.__str__` returns the address field  
**Target**         : Model – `Location`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Location` with `address="123 Health St"` (other fields can be dummy).  
  2. Call `str(location)`.  
**Expected Result**: Returns `"123 Health St"`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑005  
**Title**          : `Hospital` links correctly to a `Location` (OneToOne)  
**Target**         : Model – `Hospital` / `Location` relationship  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Location` instance.  
  2. Create a `Hospital` with `location` set to the previously created `Location`.  
  3. Retrieve `hospital.location` and compare to the original `Location`.  
**Expected Result**: The retrieved location is the same object (identical primary key).  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑006  
**Title**          : `Hospital.__str__` returns the hospital name  
**Target**         : Model – `Hospital`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Hospital` with `name="City Hospital"`.  
  2. Evaluate `str(hospital)`.  
**Expected Result**: `"City Hospital"`  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑007  
**Title**          : `Profile.get_populated_fields` excludes default birthday  
**Target**         : Model – `Profile.get_populated_fields`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Profile` with only `firstname="John"` and `lastname="Doe"` (leave `birthday` at its default `date(1000,1,1)`).  
  2. Call `profile.get_populated_fields()`.  
**Expected Result**: Returned dictionary contains keys `firstname` and `lastname` but **does not** contain `birthday`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑008  
**Title**          : `Profile.get_populated_fields` includes non‑default birthday  
**Target**         : Model – `Profile.get_populated_fields`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Profile` with `birthday=date(1990,5,20)` and other fields left blank.  
  2. Call `profile.get_populated_fields()`.  
**Expected Result**: Dictionary contains the key `birthday` with value `datetime.date(1990, 5, 20)`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑009  
**Title**          : `Profile.__str__` concatenates first and last name (including empty parts)  
**Target**         : Model – `Profile`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Create a `Profile` with `firstname="Alice"` and `lastname=""`.  
  2. Evaluate `str(profile)`.  
**Expected Result**: Returns `"Alice "` (note the trailing space).  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑010  
**Title**          : `Account.to_name` maps role integer to display name  
**Target**         : Model – `Account.to_name` (static method)  
**Preconditions**  : None  
**Test Steps**     :  
  1. Call `Account.to_name(Account.ACCOUNT_DOCTOR)`.  
**Expected Result**: Returns `"Doctor"`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑011  
**Title**          : `Account.to_name` returns “None” for unknown integer  
**Target**         : Model – `Account.to_name`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Call `Account.to_name(999)`.  
**Expected Result**: Returns `"None"`.  
**Test Type**      : Negative  

---  
**TC-ID**          : TC‑CODE‑012  
**Title**          : `Account.to_value` converts role name (case‑insensitive) to integer  
**Target**         : Model – `Account.to_value`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Call `Account.to_value("doctor")`.  
**Expected Result**: Returns `Account.ACCOUNT_DOCTOR` (20).  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑013  
**Title**          : `Account.to_value` returns 0 for invalid role string  
**Target**         : Model – `Account.to_value`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Call `Account.to_value("invalidrole")`.  
**Expected Result**: Returns `0` (`ACCOUNT_UNKNOWN`).  
**Test Type**      : Negative  

---  
**TC-ID**          : TC‑CODE‑014  
**Title**          : `Account.__str__` prefixes “Dr.” for doctor role  
**Target**         : Model – `Account.__str__`  
**Preconditions**  :  
  - A Django `User` object exists.  
  - A `Profile` object exists.  
**Test Steps**     :  
  1. Create an `Account` with `role=Account.ACCOUNT_DOCTOR`, linking the above `User` and `Profile`.  
  2. Evaluate `str(account)`.  
**Expected Result**: Returns `"Dr. <profile first> <profile last>"` (the profile’s `__str__`).  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑015  
**Title**          : `Account.__str__` does not prefix for non‑doctor roles  
**Target**         : Model – `Account.__str__`  
**Preconditions**  : Same as TC‑014, but `role=Account.ACCOUNT_PATIENT`.  
**Test Steps**     :  
  1. Create the `Account`.  
  2. Evaluate `str(account)`.  
**Expected Result**: Returns the plain profile string without “Dr.” prefix.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑016  
**Title**          : `Action.to_name` maps action integer to description  
**Target**         : Model – `Action.to_name`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Call `Action.to_name(Action.ACTION_APPOINTMENT)`.  
**Expected Result**: Returns `"Appointment"`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑017  
**Title**          : `Action.to_value` resolves case‑insensitive action name  
**Target**         : Model – `Action.to_value`  
**Preconditions**  : None  
**Test Steps**     :  
  1. Call `Action.to_value("prescription")`.  
**Expected Result**: Returns `Action.ACTION_PRESCRIPTION` (6).  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑018  
**Title**          : `Appointment.get_populated_fields` returns all fields accurately  
**Target**         : Model – `Appointment.get_populated_fields`  
**Preconditions**  :  
  - `User` / `Account` objects for a doctor and a patient exist.  
  - A `Hospital`, `Symptom` exist.  
  - Valid `datetime` values for `startTime` and `endTime`.  
**Test Steps**     :  
  1. Create an `Appointment` with all foreign keys set to the objects above and with `description="Check‑up"`, `appointment_type="Online"`.  
  2. Call `appointment.get_populated_fields()`.  
**Expected Result**: Dictionary contains keys `doctor`, `patient`, `symptom`, `description`, `hospital`, `appointment_type`, `startTime`, `endTime` each matching the instance’s values.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑019  
**Title**          : `Message` defaults for deletion flags are `False`  
**Target**         : Model – `Message`  
**Preconditions**  :  
  - Two `Account` objects (sender & target).  
**Test Steps**     :  
  1. Create a `Message` without specifying `sender_deleted` or `target_deleted`.  
  2. Retrieve the record from the DB.  
**Expected Result**: Both `sender_deleted` and `target_deleted` are `False`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑020  
**Title**          : `Notification.read_timestamp` can be null initially  
**Target**         : Model – `Notification`  
**Preconditions**  : An `Account` exists.  
**Test Steps**     :  
  1. Create a `Notification` with `read=False` and do **not** set `read_timestamp`.  
  2. Save and fetch the instance.  
**Expected Result**: `read_timestamp` is `None`.  
**Test Type**      : Functional  

---  
**TC-ID**          : TC‑CODE‑021  
**Title**          : Setting `Notification.read=True` updates `read_timestamp` (application logic not present, test that field can be set).  
**Target**         : Model – `Notification` field behavior  
**Preconditions**  : An `Account` exists.  
**Test Steps**     :  
  1. Create a `Notification` with `read=False`.  
  2. Update the instance: `